/**
 * Download firmware via serial UART
 * 
 * @param {string} deviceName - NVMe device name
 * - "aqaurius"
 * @param {string} fwPkgPath - directory path which has full firmware package files
 * 
 * @function downloadFirmwareUart
 * @memberof sonar
 */
sonar.downloadFirmwareUart = function(deviceName, fwPkgPath) {
   // [TODO] write your own firmware download algorithm   
}
